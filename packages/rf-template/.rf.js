// @rf-cli-import

module.exports = {
  // @rf-cli-config
  webpack: (config, env, base) => {
    // @rf-cli-complete-begin
    // @rf-cli-complete-end
    return config;
  },
  devServer: (config, env, base) => {
    return config;
  }
};
