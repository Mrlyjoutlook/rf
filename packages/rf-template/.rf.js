// @rf-cli-import

module.exports = {
  // @rf-cli-config
  webpack: (config, env, base, paths) => {
    // @rf-cli-complete-begin
    // @rf-cli-complete-end
    return config;
  },
  devServer: (config, env, base, paths) => {
    return config;
  }
};
