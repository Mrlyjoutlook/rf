// @rf-cli-import

module.exports = {
  // @rf-cli-config
  webpack: (config, paths, env, base) => {
    // @rf-cli-complete-begin
    // @rf-cli-complete-end
    return config;
  },
  devServer: (config, paths, env, base) => {
    return config;
  },
};
