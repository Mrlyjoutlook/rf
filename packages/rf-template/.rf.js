module.exports = {
  // @rf-cli-config
  webpack: (config, env) => {
    // @rf-cli-complete-begin
    // @rf-cli-complete-end
    return config;
  },
  devServer: (config, env) => {
    return config;
  }
};
